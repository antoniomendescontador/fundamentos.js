//operador pre-fix e unário
//++d

//operador pos-fix e unário
//d++

//operador in-fix e binário
const [a, b, c, d] = [3, 5, 1, 15]
const soma = a + b + c + d //infix e binário
const subtracao = d - b
const multiplicacao = b * c
const divisao = d / a
const modulo = a % 2

console.log(soma,subtracao,multiplicacao,divisao,modulo)